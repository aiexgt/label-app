<%- include('partials/header') %>
<style>
    .sortable-ghost { opacity: 0.3; background-color: #f7fafc; border: 1px dashed #cbd5e1; }
    .sortable-drag { cursor: grabbing !important; }
    .scroll-invisible::-webkit-scrollbar { display: none; }
    .scroll-invisible { -ms-overflow-style: none; scrollbar-width: none; }
</style>

<div class="mb-8 flex justify-between items-center">
    <h1 class="text-2xl font-semibold text-ink-900 tracking-tight">Tablero de Producción</h1>
</div>

<div class="flex flex-col md:flex-row md:overflow-x-auto space-y-5 md:space-y-0 md:space-x-5 pb-8 min-h-[75vh] scroll-invisible">
    <% 
        const columns = [
            { id: 'pendiente', name: 'Pendiente', headerBg: 'bg-status-pending', text: 'text-ink-500' },
            { id: 'imprimiendo', name: 'Imprimiendo', headerBg: 'bg-status-printing', text: 'text-indigo-600' },
            { id: 'cortando', name: 'Cortando', headerBg: 'bg-status-cutting', text: 'text-yellow-600' },
            { id: 'terminado', name: 'Terminado', headerBg: 'bg-status-finished', text: 'text-emerald-600' },
            { id: 'entregado', name: 'Entregado', headerBg: 'bg-status-delivered', text: 'text-pink-600' }
        ];
    %>
    
    <% columns.forEach(col => { %>
        <div class="w-full md:w-80 flex-shrink-0 flex flex-col rounded-xl border border-ink-300 bg-surface">
            <div class="<%= col.headerBg %> px-5 py-4 rounded-t-xl border-b border-ink-300 flex justify-between items-center transition-colors">
                <h3 class="font-medium text-sm <%= col.text %>"><%= col.name %></h3>
                <span id="count-<%= col.id %>" class="bg-white/60 backdrop-blur-sm text-ink-500 text-xs font-semibold px-2.5 py-0.5 rounded-full">
                    <%= board[col.id] ? board[col.id].length : 0 %>
                </span>
            </div>
            
            <div class="p-4 flex-1 overflow-y-auto sortable-list" data-status="<%= col.id %>">
                <% (board[col.id] || []).forEach(order => { %>
                    <div class="mb-3 bg-panel p-4 rounded-lg shadow-subtle border border-ink-300 cursor-grab hover:border-brand-300 transition-colors group relative" data-id="<%= order.id %>">
                        <div class="flex justify-between items-start mb-3">
                            <span class="text-[11px] font-mono font-medium text-ink-500">#<%= order.id %></span>
                            <span class="text-xs font-medium text-brand-600 bg-brand-50 px-2 py-1 rounded cursor-pointer hover:bg-brand-100 transition-colors" id="qty-text-<%= order.id %>" onclick="editQuantity(<%= order.id %>, <%= order.quantity %>)" title="Editar cantidad">
                                <%= order.quantity %> unid. ✏️
                            </span>
                        </div>
                        
                        <% if (user.is_admin) { %>
                            <div class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity">
                                <form action="/orders/<%= order.id %>/delete" method="POST" class="ajax-delete-form" onsubmit="return confirm('¿Seguro que deseas ELIMINAR este pedido por completo?');">
                                    <button type="submit" class="text-red-500 hover:text-red-700 bg-white rounded-full p-1 shadow-sm border border-red-100" title="Eliminar pedido">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                        </svg>
                                    </button>
                                </form>
                            </div>
                        <% } %>
                        
                        <h4 class="font-medium text-ink-900 text-sm mb-1 leading-snug"><%= order.product_name %></h4>
                        <p class="text-[11px] text-ink-500 mb-3 leading-relaxed">
                            <%= order.height %>x<%= order.width %>cm &bull; <span id="sheets-text-<%= order.id %>"><%= order.total_sheets %></span> Hojas<br>
                            <% if (order.quality_name) { %><span class="font-medium text-ink-700">Calidad:</span> <%= order.quality_name %><% } %>
                        </p>
                        
                        <% if (order.tags) { %>
                            <div class="flex flex-wrap gap-1 mb-3">
                                <% order.tags.split(',').forEach(tag => { %>
                                    <% if(tag.trim() !== '') { %>
                                        <span class="inline-flex items-center px-1.5 py-0.5 rounded text-[9px] font-bold bg-brand-50 text-brand-700 uppercase tracking-wider border border-brand-200"><%= tag.trim() %></span>
                                    <% } %>
                                <% }) %>
                            </div>
                        <% } %>
                        
                        <% if (order.operator_username) { %>
                            <div class="mb-3">
                                <span id="operator-<%= order.id %>" class="inline-flex items-center gap-1 bg-amber-50 text-amber-700 border border-amber-200 text-[10px] font-semibold px-2 py-0.5 rounded-full">
                                    👤 <%= order.operator_username %>
                                </span>
                            </div>
                        <% } %>
                        
                        <!-- Attachments -->
                        <div class="flex space-x-3 mb-4">
                            <% if (order.image_path) { %><a href="<%= order.image_path %>" target="_blank" class="text-xs text-ink-500 hover:text-brand-600 transition-colors flex items-center gap-1" onclick="event.stopPropagation()">Img</a><% } %>
                            <% if (order.pdf_path) { %><a href="<%= order.pdf_path %>" target="_blank" class="text-xs text-ink-500 hover:text-brand-600 transition-colors flex items-center gap-1" onclick="event.stopPropagation()">PDF</a><% } %>
                            <% if (order.word_path) { %><a href="<%= order.word_path %>" target="_blank" class="text-xs text-ink-500 hover:text-brand-600 transition-colors flex items-center gap-1" onclick="event.stopPropagation()">Doc</a><% } %>
                        </div>
                        <div class="mt-3 pt-3 border-t border-ink-300 relative group/obs">
                            <p class="text-[11px] text-ink-500 italic whitespace-pre-wrap" id="obs-text-<%= order.id %>"><%= order.observations || 'Sin observaciones...' %></p>
                            <button onclick="editObservations(<%= order.id %>)" class="absolute top-1 right-0 opacity-0 group-hover/obs:opacity-100 text-brand-600 hover:text-brand-800 bg-white px-1.5 py-0.5 rounded text-[10px] shadow-sm border border-brand-100 transition-opacity">✏️ Editar</button>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    <% }) %>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const lists = document.querySelectorAll('.sortable-list');
        lists.forEach(list => {
            new Sortable(list, {
                group: 'kanban',
                animation: 150,
                ghostClass: 'sortable-ghost',
                dragClass: 'sortable-drag',
                onEnd: function (evt) {
                    const itemEl = evt.item;
                    const orderId = itemEl.getAttribute('data-id');
                    const toList = evt.to;
                    const newStatus = toList.getAttribute('data-status');
                    const oldStatus = evt.from.getAttribute('data-status');
                    
                    if(newStatus !== oldStatus) {
                        fetch(`/api/orders/${orderId}/status`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ status: newStatus })
                        })
                        .then(response => response.json())
                        .then(data => {
                            if (!data.success) {
                                alert('Error al mover: ' + (data.error || 'Desconocido'));
                                evt.from.appendChild(itemEl);
                            } else {
                                const fromCountEl = document.getElementById('count-' + oldStatus);
                                const toCountEl = document.getElementById('count-' + newStatus);
                                if (fromCountEl) fromCountEl.textContent = Math.max(0, parseInt(fromCountEl.textContent) - 1);
                                if (toCountEl) toCountEl.textContent = parseInt(toCountEl.textContent) + 1;
                                
                                const opPill = document.getElementById('operator-' + orderId);
                                if (opPill && data.operator_username) {
                                    opPill.textContent = '👤 ' + data.operator_username;
                                }
                            }
                        })
                        .catch(error => {
                            console.error('Error:', error);
                            alert('Error de red');
                            evt.from.appendChild(itemEl);
                        });
                    }
                },
            });
        });
    });

    function editObservations(id) {
        const textEl = document.getElementById('obs-text-' + id);
        const currentText = textEl.textContent === 'Sin observaciones...' ? '' : textEl.textContent;
        const newText = prompt('Comentario / Observación para el pedido #' + id + ':', currentText);
        
        if (newText !== null) {
            fetch(`/api/orders/${id}/observations`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ observations: newText.trim() })
            })
            .then(res => res.json())
            .then(data => {
                if (data.success) {
                    textEl.textContent = newText.trim() || 'Sin observaciones...';
                } else {
                    alert('Error al guardar: ' + (data.error || 'Desconocido'));
                }
            })
            .catch(err => {
                console.error(err);
                alert('Error de red al guardar.');
            });
        }
    }

    function editQuantity(id, currentQty) {
        const newQtyStr = prompt('Ingresa la nueva cantidad para el pedido #' + id + ':', currentQty);
        if (newQtyStr !== null) {
            const newQty = parseInt(newQtyStr, 10);
            if (!isNaN(newQty) && newQty > 0) {
                fetch(`/api/orders/${id}/quantity`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ quantity: newQty })
                })
                .then(res => res.json())
                .then(data => {
                    if (data.success) {
                        const qtyEl = document.getElementById('qty-text-' + id);
                        if (qtyEl) {
                            qtyEl.innerHTML = data.new_quantity + ' unid. ✏️';
                            qtyEl.setAttribute('onclick', `editQuantity(${id}, ${data.new_quantity})`);
                        }
                        const sheetsEl = document.getElementById('sheets-text-' + id);
                        if (sheetsEl && data.total_sheets) {
                            sheetsEl.textContent = data.total_sheets;
                        }
                    } else {
                        alert('Error: ' + (data.error || 'Desconocido'));
                    }
                })
                .catch(err => {
                    console.error(err);
                    alert('Error de red al actualizar cantidad.');
                });
            } else {
                alert('Por favor ingresa una cantidad válida mayor a 0.');
            }
        }
    }
</script>
<%- include('partials/footer') %>
